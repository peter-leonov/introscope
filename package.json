{
    "name": "introscope",
    "version": "1.0.15",
    "description": "Babel plugin which helps with ES modules testing",
    "repository": "github:peter-leonov/introscope",
    "author": "Peter Leonov",
    "license": "MIT",
    "scripts": {
        "test": "jest && flow check",
        "deploy": "yarn test && npm version patch && npm publish && git push && git push --tags"
    },
    "peerDependencies": {
        "babel-core": "^6.26.0"
    },
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-core": "^6.26.0",
        "babel-jest": "^22.4.1",
        "babel-plugin-syntax-flow": "^6.18.0",
        "babel-plugin-syntax-object-rest-spread": "^6.13.0",
        "babel-preset-env": "^1.6.1",
        "flow-bin": "^0.68.0",
        "jest": "^22.4.2",
        "regenerator-runtime": "^0.11.1"
    }
}
